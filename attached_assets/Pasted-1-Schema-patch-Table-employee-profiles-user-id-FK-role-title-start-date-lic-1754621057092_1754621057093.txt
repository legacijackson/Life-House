1️⃣  **Schema patch**
   • Table `employee_profiles`  
     (user_id FK, role_title, start_date, license_number, license_expiry,
      emergency_contact, bank_routing, bank_account,
      w4_signed, i9_signed, bg_check_status, created_at).  
   • Junction tables `cm_residents` and `cm_properties`
     so Admin can assign caseload during or after onboarding.

2️⃣  **E-sign bundle scaffold**
   • Folder `/docs/hr/` with placeholder PDFs:  
     EmploymentAgreement.pdf, HIPAA_BAA.pdf, StaffRules.pdf,
     TechPolicy.pdf, MediaRelease.pdf, I-9.pdf, W-4.pdf.

3️⃣  **Wizard UI stubs**
   • `pages/onboard/cm/[token].tsx` (8-step layout).  
   • Components: `EsignCanvas`, `CaseloadAssign`, `WizardProgress`.

4️⃣  **API skeleton**
   • `/admin/onboard` POST (creates inactive CM + token).  
   • `/admin/onboard/:token` PATCH (save step).  
   • `/admin/onboard/finalize` POST (activate user).

Create migration files, seed nothing else, commit one atomic change.  
Output format:

📦 Commit Ready  
Added: [list]  
Modified: [list]  
<<<PROMPT-1-END>>>
PROMPT 2 — BUILD (→ Replit Advanced Assistant)
pgsql
Copy
Edit
<<<PROMPT-2-START>>>
# Build Phase – Case-Manager Onboarding Wizard

✅  PRE-CHECK  
• `git status` clean, `DATABASE_URL` set.

### Task Matrix
1. **Migrations**
   ◻ Run SQL generated by Architect; deploy via `npx prisma migrate deploy`.

2. **Seed Admin Endpoint**
   ◻ POST /admin/onboard creates token email to CM (SendGrid test).

3. **Wizard Implementation** (8 screens)
   1. Personal Info
   2. Credentials & License
   3. Tax & Banking  (store encrypted `bank_account`,`bank_routing`)
   4. Background-Check Consent & PDF upload
   5. E-Sign Packet  (multi-tab EsignCanvas; require two ID images + selfie)
   6. Emergency Contact
   7. Assign Caseload (multiselect Residents, Properties pulled via API)
   8. Review & Submit → calls `/admin/onboard/finalize`

4. **Access Control**
   • CM account `active=false` until finalize route sets true.  
   • Inactive CMs blocked from login.

5. **Notifications**
   • Slack `#help-tickets`: “🎉 CM Onboarding Complete – {name}”.  
   • SendGrid welcome email to CM.

6. **Reporting**
   • Add employee fields to existing `/reports/employees` CSV/PDF.

7. **Tests**
   • F-67  Completes wizard happy-path.  
   • B-API-11  Finalize without i-9_signed should 400.  
   • DB-11  `employee_profiles` row created & user.active = true.

8. **Image Crop + S3**
   • Use `react-easy-crop` for profile pic; upload to DigitalOcean Spaces.

Run all tests, lint, build; output  
`BUILD COMPLETE – 0 errors` or failing specs inline.
<<<PROMPT-2-END>>>
PROMPT 3 — QA & CLEAN (→ Replit Advanced Assistant)
yaml
Copy
Edit
<<<PROMPT-3-START>>>
# QA + Clean – Case-Manager Onboarding Wizard

1. Purge mock data; keep only admin accounts.
2. Run tests F-67, B-API-11, DB-11 plus lint & audit.
3. Simulate real flow:
   • Admin creates CM token → complete wizard →
     Slack & welcome email received.  
   • Verify user.active = true, employee_profiles populated,
     caseload links present.
4. Ensure no high vulns (`npm audit`, ZAP), no secrets leak.
5. Package artifacts to /artifacts/cm_onboarding/.

Success output:
QA PASS ✅ (wizard)

Failure output:
QA FAIL ❌ | step | reason |
<<<PROMPT-3-END>>>